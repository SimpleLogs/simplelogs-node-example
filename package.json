{
  "name": "simplelogs-node-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SimpleLogs in a framework-free Node server",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js"
  },
  "dependencies": {
    "@simplelogs/node": "^2.0.0"
  }
}
